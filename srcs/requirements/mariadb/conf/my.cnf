[mysqld]
# This is the MariaDB configuration file.

# Change bind-address to allow external connections.
bind-address=0.0.0.0

# Security settings
skip-networking=0
skip-grant-tables=0

# Ensure proper authentication
default-authentication-plugin=mysql_native_password


